<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aaa.dao.ReportDao">

    <resultMap type="com.aaa.entity.Report" id="ReportMap">
        <result property="reportid" column="reportId" jdbcType="INTEGER"/>
        <result property="reportname" column="reportName" jdbcType="VARCHAR"/>
        <result property="sex" column="sex" jdbcType="VARCHAR"/>
        <result property="age" column="age" jdbcType="INTEGER"/>
        <result property="department" column="department" jdbcType="INTEGER"/>
        <result property="doctor" column="doctor" jdbcType="INTEGER"/>
        <result property="reporttype" column="reportType" jdbcType="INTEGER"/>
        <result property="price" column="price" jdbcType="VARCHAR"/>
        <result property="time" column="time" jdbcType="TIMESTAMP"/>
        <result property="users" column="users" jdbcType="VARCHAR"/>
        <result property="state" column="state" jdbcType="INTEGER"/>
        <result property="phone" column="phone" jdbcType="VARCHAR"/>
        <result property="carid" column="carid" jdbcType="VARCHAR"/>
        <result property="zhuan" column="zhuan" jdbcType="VARCHAR"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="ReportMap">
        select
          reportId, reportName, sex, age, department, doctor, reportType, price, time, users, state, phone, carid, zhuan
        from report
        where reportId = #{reportid}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="ReportMap">
        select
          reportId, reportName, sex, age, department, doctor, reportType, price, time, users, state, phone, carid, zhuan
        from report
        <where>
            <if test="reportid != null">
                and reportId = #{reportid}
            </if>
            <if test="reportname != null and reportname != ''">
                and reportName = #{reportname}
            </if>
            <if test="sex != null and sex != ''">
                and sex = #{sex}
            </if>
            <if test="age != null">
                and age = #{age}
            </if>
            <if test="department != null">
                and department = #{department}
            </if>
            <if test="doctor != null">
                and doctor = #{doctor}
            </if>
            <if test="reporttype != null">
                and reportType = #{reporttype}
            </if>
            <if test="price != null">
                and price = #{price}
            </if>
            <if test="time != null">
                and time = #{time}
            </if>
            <if test="users != null and users != ''">
                and users = #{users}
            </if>
            <if test="state != null">
                and state = #{state}
            </if>
            <if test="phone != null and phone != ''">
                and phone = #{phone}
            </if>
            <if test="carid != null and carid != ''">
                and carid = #{carid}
            </if>
            <if test="zhuan != null and zhuan != ''">
                and zhuan = #{zhuan}
            </if>
        </where>
        limit #{pageable.offset}, #{pageable.pageSize}
    </select>

    <!--统计总行数-->
    <select id="count" resultType="java.lang.Long">
        select count(1)
        from report
        <where>
            <if test="reportid != null">
                and reportId = #{reportid}
            </if>
            <if test="reportname != null and reportname != ''">
                and reportName = #{reportname}
            </if>
            <if test="sex != null and sex != ''">
                and sex = #{sex}
            </if>
            <if test="age != null">
                and age = #{age}
            </if>
            <if test="department != null">
                and department = #{department}
            </if>
            <if test="doctor != null">
                and doctor = #{doctor}
            </if>
            <if test="reporttype != null">
                and reportType = #{reporttype}
            </if>
            <if test="price != null">
                and price = #{price}
            </if>
            <if test="time != null">
                and time = #{time}
            </if>
            <if test="users != null and users != ''">
                and users = #{users}
            </if>
            <if test="state != null">
                and state = #{state}
            </if>
            <if test="phone != null and phone != ''">
                and phone = #{phone}
            </if>
            <if test="carid != null and carid != ''">
                and carid = #{carid}
            </if>
            <if test="zhuan != null and zhuan != ''">
                and zhuan = #{zhuan}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="reportid" useGeneratedKeys="true">
        insert into report(reportName, sex, age, department, doctor, reportType, price, time, users, state, phone, carid, zhuan)
        values (#{reportname}, #{sex}, #{age}, #{department}, #{doctor}, #{reporttype}, #{price}, #{time}, #{users}, #{state}, #{phone}, #{carid}, #{zhuan})
    </insert>

    <insert id="insertBatch" keyProperty="reportid" useGeneratedKeys="true">
        insert into report(reportName, sex, age, department, doctor, reportType, price, time, users, state, phone, carid, zhuan)
        values
        <foreach collection="entities" item="entity" separator=",">
        (#{entity.reportname}, #{entity.sex}, #{entity.age}, #{entity.department}, #{entity.doctor}, #{entity.reporttype}, #{entity.price}, #{entity.time}, #{entity.users}, #{entity.state}, #{entity.phone}, #{entity.carid}, #{entity.zhuan})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="reportid" useGeneratedKeys="true">
        insert into report(reportName, sex, age, department, doctor, reportType, price, time, users, state, phone, carid, zhuan)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.reportname}, #{entity.sex}, #{entity.age}, #{entity.department}, #{entity.doctor}, #{entity.reporttype}, #{entity.price}, #{entity.time}, #{entity.users}, #{entity.state}, #{entity.phone}, #{entity.carid}, #{entity.zhuan})
        </foreach>
        on duplicate key update
        reportName = values(reportName),
        sex = values(sex),
        age = values(age),
        department = values(department),
        doctor = values(doctor),
        reportType = values(reportType),
        price = values(price),
        time = values(time),
        users = values(users),
        state = values(state),
        phone = values(phone),
        carid = values(carid),
        zhuan = values(zhuan)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update report
        <set>
            <if test="reportname != null and reportname != ''">
                reportName = #{reportname},
            </if>
            <if test="sex != null and sex != ''">
                sex = #{sex},
            </if>
            <if test="age != null">
                age = #{age},
            </if>
            <if test="department != null">
                department = #{department},
            </if>
            <if test="doctor != null">
                doctor = #{doctor},
            </if>
            <if test="reporttype != null">
                reportType = #{reporttype},
            </if>
            <if test="price != null">
                price = #{price},
            </if>
            <if test="time != null">
                time = #{time},
            </if>
            <if test="users != null and users != ''">
                users = #{users},
            </if>
            <if test="state != null">
                state = #{state},
            </if>
            <if test="phone != null and phone != ''">
                phone = #{phone},
            </if>
            <if test="carid != null and carid != ''">
                carid = #{carid},
            </if>
            <if test="zhuan != null and zhuan != ''">
                zhuan = #{zhuan},
            </if>
        </set>
        where reportId = #{reportid}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from report where reportId = #{reportid}
    </delete>

</mapper>

