<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aaa.dao.InhospitaltypeDao">

    <resultMap type="com.aaa.entity.Inhospitaltype" id="InhospitaltypeMap">
        <result property="inhospitalid" column="inhospitalId" jdbcType="INTEGER"/>
        <result property="projectname" column="projectName" jdbcType="VARCHAR"/>
        <result property="unit" column="unit" jdbcType="INTEGER"/>
        <result property="price" column="price" jdbcType="VARCHAR"/>
        <result property="bigprojectid" column="bigprojectId" jdbcType="INTEGER"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="InhospitaltypeMap">
        select
          inhospitalId, projectName, unit, price, bigprojectId
        from inhospitaltype
        where inhospitalId = #{inhospitalid}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="InhospitaltypeMap">
        select
          inhospitalId, projectName, unit, price, bigprojectId
        from inhospitaltype
        <where>
            <if test="inhospitalid != null">
                and inhospitalId = #{inhospitalid}
            </if>
            <if test="projectname != null and projectname != ''">
                and projectName = #{projectname}
            </if>
            <if test="unit != null">
                and unit = #{unit}
            </if>
            <if test="price != null">
                and price = #{price}
            </if>
            <if test="bigprojectid != null">
                and bigprojectId = #{bigprojectid}
            </if>
        </where>
        limit #{pageable.offset}, #{pageable.pageSize}
    </select>

    <!--统计总行数-->
    <select id="count" resultType="java.lang.Long">
        select count(1)
        from inhospitaltype
        <where>
            <if test="inhospitalid != null">
                and inhospitalId = #{inhospitalid}
            </if>
            <if test="projectname != null and projectname != ''">
                and projectName = #{projectname}
            </if>
            <if test="unit != null">
                and unit = #{unit}
            </if>
            <if test="price != null">
                and price = #{price}
            </if>
            <if test="bigprojectid != null">
                and bigprojectId = #{bigprojectid}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="inhospitalid" useGeneratedKeys="true">
        insert into inhospitaltype(projectName, unit, price, bigprojectId)
        values (#{projectname}, #{unit}, #{price}, #{bigprojectid})
    </insert>

    <insert id="insertBatch" keyProperty="inhospitalid" useGeneratedKeys="true">
        insert into inhospitaltype(projectName, unit, price, bigprojectId)
        values
        <foreach collection="entities" item="entity" separator=",">
        (#{entity.projectname}, #{entity.unit}, #{entity.price}, #{entity.bigprojectid})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="inhospitalid" useGeneratedKeys="true">
        insert into inhospitaltype(projectName, unit, price, bigprojectId)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.projectname}, #{entity.unit}, #{entity.price}, #{entity.bigprojectid})
        </foreach>
        on duplicate key update
        projectName = values(projectName),
        unit = values(unit),
        price = values(price),
        bigprojectId = values(bigprojectId)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update inhospitaltype
        <set>
            <if test="projectname != null and projectname != ''">
                projectName = #{projectname},
            </if>
            <if test="unit != null">
                unit = #{unit},
            </if>
            <if test="price != null">
                price = #{price},
            </if>
            <if test="bigprojectid != null">
                bigprojectId = #{bigprojectid},
            </if>
        </set>
        where inhospitalId = #{inhospitalid}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from inhospitaltype where inhospitalId = #{inhospitalid}
    </delete>

</mapper>

